#include <stdio.h>

#define BUFFER_SIZE 5

int main(void)
{
    /*int buffer[BUFFER_SIZE];

    for (int i = 0; i < BUFFER_SIZE; i++)
    {
        printf("Enter: ");
        scanf("%d", &buffer[i]);
    }
    int total = 0;
    for (int i = 0; i < BUFFER_SIZE; i ++)
    {
        printf("buffer[%d] = %d\n", i, buffer[i]);
        total += buffer[i];
    }
    printf("Total: %d", total);
    */
    
    return 0;
}